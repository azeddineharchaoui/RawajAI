# 1. CLEAN UNINSTALL any pre-installed conflicting versions
!pip uninstall -y numpy pandas scipy statsmodels matplotlib seaborn

# 2. STABLE SCIENTIFIC STACK (always install this first!)
!pip install \
  numpy==1.26.4 \
  pandas==2.2.2 \
  scipy==1.13.1 \
  statsmodels==0.14.1 \
  matplotlib==3.7.1 \
  seaborn==0.13.2


# 3. LANGCHAIN ECOSYSTEM — must match versions
!pip install \
  langchain==0.2.17 \
  langchain-core==0.3.72 \
  langchain-community==0.2.19 \
  langchain-huggingface==0.2.1 \
  langchain-text-splitters==0.2.0

# 4. TRANSFORMERS + DEEP LEARNING stack
!pip install \
  transformers \
  torch \
  torchaudio \
  sentencepiece \
  accelerate \
  bitsandbytes \
  autoawq \
  reportlab

# 5. MISC. NLP / UTILITY PACKAGES
!pip install \
  faiss-cpu \
  protobuf \
  pulp \
  gtts \
  pytrends \
  pydub==0.25.1 \
  soundfile==0.12.1 \
  ffmpeg-python==0.2.0

# 6. VISUALIZATION & FORECASTING
!pip install \
  plotly==5.24.1 \
  kaleido==0.2.1 \
  prophet  # requires numpy/scipy already present!

# 7. FLASK & TEXT UTILS
!pip install \
  Flask \
  Flask-CORS==4.0.0 \
  langdetect \
  pypdf==3.17.0

import os
os.kill(os.getpid(), 9)